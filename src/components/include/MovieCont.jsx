import React from 'react'

const MovieText = ({name}) => {
    return (
      <div></div>
    );
  };

const MovieCont = ({movies}) => {
    console.log({movies})
  return (
    <section></section>
  )
}

export default MovieCont